{
  "name": "sample-node-app",
  "version": "1.0.0",
  "description": "Demo application for GitHub Actions CI Pipeline",
  "scripts": {
    "lint": "echo \"Lint successful\"",
    "test": "echo \"All unit tests passed\"",
    "build": "mkdir -p dist && cp index.js dist/index.js"
  }
}
